# Pasting images in emacs

`M-x yank-media`

Also seems to offer conversions between different image types, or at least there's a question for the filetype when pasting. Also accepts an alt text in at least markdown-mode, and in markdown-mode, the paste is in markdown syntax. Pretty neat how it just works. I forked and rewrote an entire plugin to get it vaguely working in vim
